module
    auth:
        ├ register
        ├ login
        ├ verify JWT
        └ logout / refresh token
    


note: register
        ↓
      login
        ↓
    server tạo JWT
        ↓
    client gửi JWT mỗi request
        ↓
    server verify token

authMiddleware → xác thực token (authentication)
roleMiddleware → kiểm tra quyền (authorization)


users/me: 
Client
↓
Authorization: Bearer TOKEN
↓
auth.middleware
↓
jwt.verify()
↓
req.user = decoded
↓
controller
↓
service
↓
database

-------------------------
// api: /auth
/users
/freelancers
/gigs
/orders
/reviews
/messages
/conversations
--------------------
LUỒNG user thành freelancer:
login
↓
JWT
↓
authMiddleware
↓
req.user
↓
createFreelancer